{
  "name": "client-onboarding-workflow",
  "version": "1.0.0",
  "description": "Automated client onboarding workflow using Trigger.dev, Claude AI, and n8n",
  "private": true,
  "scripts": {
    "dev": "npx trigger.dev@latest dev",
    "deploy": "trigger deploy",
    "server": "tsx watch server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.52.0",
    "@hono/node-server": "^1.13.0",
    "@trigger.dev/sdk": "^4.0.0",
    "hono": "^4.6.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@trigger.dev/build": "^4.0.0",
    "@types/node": "^22.0.0",
    "trigger.dev": "^4.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
